{
	"$schema": "https://turbo.build/schema.json",
	"pipeline": {
		"compile": {
			"inputs": ["src/**", "tsconfig.json", "package.json"],
			"outputs": ["lib/**", "tmp/**"]
		},
		"test": {
			"inputs": ["test/**"]
		}
	}
}
